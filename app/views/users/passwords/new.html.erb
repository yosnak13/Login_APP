<h2>パスワード再設定</h2>
<p>登録時のメールアドレスを入力してください。<br>
パスワード再設定用のメールをお送りします。</p>

<hr>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

  <div class="field">
    <!--<%= f.label :メールアドレス %><br />-->
    <%= f.email_field :email, autofocus: true, autocomplete: "email",
    class:'#{"is-invalid" if resource.errors.messages[:email].present? }', placeholder: "メールアドレス" %>
    <% if resource.errors.messages[:email].present? %>
      <strong><%= resource.errors.full_messages_for(:email).first %></strong>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit "送信" %>
  </div>
<% end %>
